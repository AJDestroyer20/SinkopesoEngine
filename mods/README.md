# Mods Directory

Each folder inside `mods/` is treated as a standalone mod package.

The compatibility scanner attempts to detect the source engine layout automatically:

- Psych Engine
- ALE Psych Engine
- Codename Engine
- V-Slice
- Native Sinkopeso format

Keep original folder structures whenever possible to maximize automatic compatibility.
